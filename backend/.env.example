PORT=3000
DB_HOST=localhost
DB_PORT=5432
DB_USER=your_postgres_username
DB_PASSWORD=your_postgres_password
DB_NAME=sun_safety
# DB_SSL=false for local PostgreSQL, true for managed cloud PostgreSQL if required
DB_SSL=false

# CORS: comma-separated list of allowed frontend origins (production only)
# e.g. https://d1234abcd.cloudfront.net,https://yourdomain.com
CORS_ORIGIN=

# Admin gate credentials (required in all non-local environments)
ADMIN_USERNAME= TP47
ADMIN_PASSWORD= 66668888
